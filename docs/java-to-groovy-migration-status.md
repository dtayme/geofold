<!--
SPDX-License-Identifier: AGPL-3.0-or-later
Copyright (C) 2026 FOGNETX <Drew.Taylor@fognetx.com>
-->

# Java To Groovy Protocol Migration Status

Status date: 2026-06-16

The migration goal is feature-complete Groovy protocol coverage from source
documentation, with archived Java parity used only as supporting evidence.

## Completed In This Pass

| Protocol | Result |
| --- | --- |
| `easytrack` | Migrated to `drivers/easytrack.groovy`, backed by Java-parity test fixtures (OBD, cell-tower, location, E3+4G model variants, TX/MQ echo suppression). Java source, encoder, and old Java protocol tests moved to `archived-protocols/easytrack/`. Requires DSL extension: `session.model` for model-aware decode. |
| `wialon` | Migrated to `drivers/wialon.groovy` covering L/P/D/SD/B/M message types, outer IMEI prefix stripping, full/short position patterns with NA fallback, extended param block (type:1/2/3 params, bat/temp remapping, cell towers up to 9 towers), batch with `KEY_ARCHIVE`, and 4 commands. Java source, encoder, and old Java protocol test moved to `archived-protocols/wialon/`. Uses `ctx.store()` for IMEI persistence after login. |
| `cartrack` | Migrated to `drivers/cartrack.groovy`, backed by the downloaded CarTrack GTP PDF, driver decode tests, command-declaration tests, and command encoder tests. Java source and old Java protocol test moved to `archived-protocols/cartrack/`. |
| `cityeasy` | Migrated to `drivers/cityeasy.groovy`, backed by the downloaded Cityeasy protocol PDF, driver decode tests, command-declaration tests, and command encoder tests. Java source, encoder, and old Java protocol test moved to `archived-protocols/cityeasy/`. |
| `enfora` | Migrated to `drivers/enfora.groovy`, backed by the downloaded Enfora API and Mini-MT PDFs, driver decode tests, command-declaration tests, and command encoder tests. Java source, encoder, and old Java protocol test moved to `archived-protocols/enfora/`. |
| `gpsgate` | Migrated to `drivers/gpsgate.groovy`, backed by the downloaded GpsGate Server protocol PDF and Java-parity driver decode tests. Java source and old Java protocol test moved to `archived-protocols/gpsgate/`. |
| `m2m` | Migrated to `drivers/m2m.groovy`, backed by the downloaded M2M protocol spreadsheet and Java-parity driver decode tests. Java source and old Java protocol test moved to `archived-protocols/m2m/`. |
| `mxt` | Migrated to `drivers/mxt.groovy`, backed by the downloaded MXT protocol PDF and Java-parity driver decode tests. Java source, frame decoder, and old Java protocol test moved to `archived-protocols/mxt/`. |
| `noran` | Migrated to `drivers/noran.groovy`, backed by downloaded Noran protocol documents, driver decode tests, command-declaration tests, and command encoder tests. Java source, encoder, and old Java protocol test moved to `archived-protocols/noran/`. |
| `orion` | Migrated to `drivers/orion.groovy`, backed by the downloaded Orion binary protocol PDF, scripted-frame driver tests, ACK coverage, and Java-parity position fixtures. Java source, frame decoder, and old Java protocol test moved to `archived-protocols/orion/`. |
| `ramac` | Migrated to `drivers/ramac.groovy`, backed by the downloaded Ramac callback PDF, HTTP driver decode tests, and JSON acknowledgement coverage. Java source and old Java protocol test moved to `archived-protocols/ramac/`. |
| `riti` | Migrated to `drivers/riti.groovy`, backed by the downloaded Riti Air Communication Protocol PDF and Java-parity driver decode tests. Java source and old Java protocol test moved to `archived-protocols/riti/`. |
| `skypatrol` | Migrated to `drivers/skypatrol.groovy`, backed by the downloaded SkyPatrol position report PDF and Java-parity driver decode tests. Java source and old Java protocol test moved to `archived-protocols/skypatrol/`. |
| `stl060` | Migrated to `drivers/stl060.groovy`, backed by the downloaded STL060 protocol document and Java-parity driver decode tests. Java source, frame decoder, and old Java protocol test moved to `archived-protocols/stl060/`. |
| `t800x` | Migrated to `drivers/t800x.groovy`, backed by the downloaded Topflytech/T800X protocol documents, driver decode tests, command-declaration tests, and command encoder tests. Java source, encoder, and old Java protocol tests moved to `archived-protocols/t800x/`. |
| `thinkpower` | Migrated to `drivers/thinkpower.groovy`, backed by the downloaded ThinkPower tracker protocol PDF and Java-parity driver decode tests. Java source and old Java protocol test moved to `archived-protocols/thinkpower/`. |
| `v680` | Migrated to `drivers/v680.groovy`, backed by the downloaded V680 GPRS protocol PDF and Java-parity driver decode tests. Java source and old Java protocol test moved to `archived-protocols/v680/`. |
| `khd` | Migrated to `drivers/khd.groovy` covering binary `0x29 0x29` framing with `readLengthField(3, 2)`, BCD date/coordinate helpers, dual device-ID lookup (hex and decimal-with-offset), MSG_LOGIN/CONFIRMATION/POSITION/ALARM/PERIPHERAL/REPLY message types, peripheral subtypes (fuel, temp, driver ID, multi-fuel, battery, cell towers), and 7 commands. Java source, encoder, and old Java protocol tests moved to `archived-protocols/khd/`. |
| `xirgo` | Migrated to `drivers/xirgo.groovy` covering `##`-terminated text framing, auto-detection of old vs new fixed format (per-channel store), custom configurable field-name form, UDP ACK, event→alarm/ignition/motion mapping, and output-control command. Java source, encoder, and old Java protocol tests moved to `archived-protocols/xirgo/`. |
| `minifinder` | Migrated to `drivers/minifinder.groovy` covering `;`-terminated text framing, channel-session login (`!1`), message types !3/!4/!5/!A/!B/!C/!D, flag-word decoding (validity/alarms/RSSI/charge), and 11 commands (timezone, voice-monitoring, speed/geofence/vibration/fall alarms, AGPS, power-saving/deep-sleep modes, SOS number, indicator). Java source, encoder, and old Java protocol tests moved to `archived-protocols/minifinder/`. |
| `tramigo` | Migrated to `drivers/tramigo.groovy` covering scripted-frame detection (type-dependent LE/BE length field), three protocol variants (0x01 fixed-binary position, 0x04 TLV-loop with CRC16-CCITT ACK, 0x80 binary-header + ASCII-text with regex coordinate/direction/time parsing). Java source, frame decoder, and old Java protocol test moved to `archived-protocols/tramigo/`. |

| `xexun` | Migrated to `drivers/xexun.groovy` covering scriptedFrame with `FrameBuffer.indexOf(String)` for GPRMC/GNRMC scan, NMEA coordinate parsing via lazy `(\d*?)(\d?\d\.\d+)` groups, both basic and full deployment modes (serial/phone prefix, sats/alt/power suffix), alarm/ignition mapping, and 2 commands (ENGINE_STOP, ENGINE_RESUME). Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/xexun/`. |

| `jmak` | Migrated to `drivers/jmak.groovy` covering three frame types on the same port (balanced-brace JSON `{…}`, `^…$` keep-alive, `~…$` position), 64-bit mask-based field parsing in decodeEvent (bits 0–34 via `BitUtil.check`), CAN/OBD section in decodeCan (bits 0–14), ACK "ACK" for all types, driver ID from eventId/eventStatus combo (126/4), and IO bitmask decode. Java source, frame decoder, and old Java protocol tests moved to `archived-protocols/jmak/`. |

| `navigil` | Migrated to `drivers/navigil.groovy` covering scriptedFrame with optional 4-byte preamble detection (LE 0x2477F5F6), length field at byte-offset 6, GPS-to-UTC leap-second correction (−25 s), CRC-16/CCITT-FALSE ACK when flags bit 0 is clear, and 6 message types: MSG_UNIT_REPORT (8), MSG_TG2_REPORT (12), MSG_POSITION_REPORT (13, 3-byte MediumLE coords), MSG_POSITION_REPORT_2 (15), MSG_SNAPSHOT4 (17), MSG_TRACKING_DATA (18). Java source, frame decoder, and old Java protocol tests moved to `archived-protocols/navigil/`. |

| `xexun2` | Migrated to `drivers/xexun2.groovy` covering escape-delimited binary framing (0xfb 0xbf escape sequences), IP-style byte-sum checksum, batch position records with optional alarm/GPS (float and double variants)/WiFi/cell-tower/ToF/speed/course/'G'-type extended GPS blocks, and 4 commands (CUSTOM, POSITION_PERIODIC, POWER_OFF, REBOOT_DEVICE). Java source, frame decoder/encoder, protocol encoder, and old Java protocol tests moved to `archived-protocols/xexun2/`. |
| `startek` | Migrated to `drivers/startek.groovy` covering variable-length ASCII scriptedFrame (length field at offset 3, total = comma_offset + 4 + length), DOTALL outer regex for IMEI/type/content extraction with checksum strip, type 000 position decoding (event/alarm, YYMMDDHHMMSS date, cell-tower, status, inputs/outputs, power/battery/ADC, fuel, temperature with 15-bit signed encoding, OBD pipe-separated fields, 20+-char driver ID, hours), type 710 serial decode (T1 engine data by direct field index, T2 trip counters), default pass-through (KEY_RESULT), and 4 commands (CUSTOM, OUTPUT_CONTROL, ENGINE_STOP, ENGINE_RESUME). Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/startek/`. |
| `huasheng` | Migrated to `drivers/huasheng.groovy` covering 0xC0-delimited binary framing with SLIP-style escape decoding (0xDB 0xDC → 0xC0, 0xDB 0xDD → 0xDB), MSG_LOGIN TLV loop for IMEI (subtype 0x0003) with login ACK, MSG_HSO_REQ heartbeat ACK, MSG_POSITION fixed header (status/event/YYMMDDHHMMSS time/lon/lat/speed/course/alt) plus TLV extensions (OBD 0x0001 with adBlueLevel/fuelLevel2, RSSI/HDOP 0x0005, VIN 0x0009, odometer 0x0010, hours 0x0011, engine data 0x0014, text-format cell towers 0x0020, WiFi 0x0021), MSG_UPFAULT DTC string with P/C/B/U prefix nibble encoding, and 5 commands (POSITION_PERIODIC, OUTPUT_CONTROL, ALARM_ARM, ALARM_DISARM, SET_SPEED_LIMIT). Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/huasheng/`. |
| `cellocator` | Migrated to `drivers/cellocator.groovy` covering type-based fixed-frame-length scriptedFrame (type at offset 4, lengths 70/31/70/variable for types 0/3/7/8/9/11), alternative mode detection (byte 3 ≠ 'P'), MSG_CLIENT_STATUS decode (version/status/event/alarm/input-bitmask/ADC/3-byte odometer/driver-ID/satellite/coord in radians-or-degrees/time reversed-byte-order), MSG_CLIENT_MODULAR_EXT module loop (types 2 IO, 6 GPS, 7 time) with endIndex tracking via consumed counter, binary ACKs for STATUS/MODULAR_EXT (MCGP frame with byte checksum from offset 4), and TYPE_OUTPUT_CONTROL command. Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/cellocator/`. |
| `watch` | Migrated to `drivers/watch.groovy` covering balanced-bracket scriptedFrame with escape sequence unescape (}01–}05), optional index field detection (5-part split, 4-hex-digit check), position pattern with 20 capture groups (status=g(19), trailing=g(20)), cell/WiFi network decode with hex LAC/CID support and wifi-first format skip, health types PULSE/HEART/BLOOD/BPHRT/TEMP/btemp2/oxygen, LK heartbeat with steps/battery (≥3 vals) or null (<3 vals), media types (img/TK/JXTK) returning null, 15 command types with CS manufacturer default, and ctx.store() for per-channel manufacturer/hasIndex/indexField ACK state. Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/watch/`. |
| `megastek` | Migrated to `drivers/megastek.groovy` covering dual-frame-format scriptedFrame (4-digit length prefix or `\r\n`/`!`/`\n` delimiter), two message families: new `$MGV` format with comma-split field parsing (fixed fields 0–23, branch-detection for binary/integer IO sections, optional heart-rate, temperatures, RFID, charge/belt, battery, alert, WiFi AP list) and old `STX`/`LOGSTX` format with simple vs. non-simple detection (GPRMC location + PATTERN_SIMPLE/PATTERN_ALTERNATIVE status blocks). Java backtracking for charge/belt vs. battery replicated via lookahead on next field. Java source, frame decoder, and old Java protocol test moved to `archived-protocols/megastek/`. |
| `gps103` | Migrated to `drivers/gps103.groovy` covering scriptedFrame for `\r\n`/`\n`/`;`/`*` delimiters, handshake (`imei:` + length ≤ 30 → reply "LOAD"), digit-prefix stripping (reply "ON"), and three decode paths: `decodeRegular` (30-group regex with L/F branch, N/S before-or-after lat, optional timezone UTC correction via local − UTC delta clamped to ±12 h, fuel/temp/IO fields), `decodeObd` (18-group OBD2 regex with engine load, coolant, throttle, RPM, battery, DTCs), `decodeAlternative` (22-group fixed CSV with HMS+DMY time, signed coords, RSSI, satellite, HDOP). SOS "help me" sends `**,imei:IMEI,E;` ACK. Photo packets (`vr` at offset 21) return null. 9 commands (CUSTOM, POSITION_SINGLE/PERIODIC/STOP, ENGINE_STOP/RESUME, ALARM_ARM/DISARM, REQUEST_PHOTO). Java source, encoder, and old Java protocol test moved to `archived-protocols/gps103/`. |
| `topin` | Migrated to `drivers/topin.groovy` covering CRLF-delimited scriptedFrame, MSG_LOGIN (8-byte BCD IMEI via `readHex(8).substring(1)`, login ACK + time-update response), MSG_GPS/GPS_OFFLINE (inlined Gt06 GPS block — 6-byte date+time, sats/lat/lon/speed/flags, optional altitude+alarm, 6-byte time echo ACK), MSG_GPS_2/GPS_OFFLINE_2 (raw byte date+time, custom 4-byte coordinate with sign in high nibble, skip 8 redundant bytes), MSG_STATUS (battery/fw/tz/interval + optional rssi/temp(skip)/charge/heartrate guarded by `readableBytes() >= N+2`), MSG_WIFI/WIFI_OFFLINE/LBS_WIFI/LBS_WIFI_2/LBS_WIFI_OFFLINE (6-byte BCD time, `length`-count WiFi APs with MAC+RSSI, cell towers with MCC/MNC/LAC/CID/RSSI, optional alarm), MSG_VIBRATION (vibration alarm), MSG_SOS_ALARM (SOS alarm), MSG_TIME_UPDATE (time-update reply only). 1 command (SOS_NUMBER: `0x41` frame with ASCII phone number). Java source, encoder, and old Java protocol tests moved to `archived-protocols/topin/`. |
| `minifinder2` | Migrated to `drivers/minifinder2.groovy` covering `readLengthFieldLE(2, 2, 4)` framing (total = 8 + LE length field), CRC16-XMODEM ACK when flags bit 4 set (MSG_RESPONSE default), MSG_DATA/MSG_SERVICES TLV loop with HashSet duplicate-key detection for batch positions via `ctx.emit()`, `buf.slice(length)` for safe TLV bounds, keys: 0x01 IMEI/0x02 alarm-bitmask (32 bits: low-battery/overspeed/fall/geofence×4/power-off/power-on/movement/SOS/btn1/btn2/bark + optional device-time)/0x14 battery/0x20 GPS (latLE/lonLE/speed/course/alt/hdop/odometer/sats)/0x21+0x29 cell towers (2LE or 4LE CID)/0x19+0x22 WiFi APs (0x19 with name skip)/0x24 status (timestamp+charge/archive/motion/RSSI/battery-level bits)/0x25 call/0x27 BLE-location/0x28+0x2c repeatable beacon tags (flags/tagId/rssi/1mRssi/optional-battery+location+description+temp)/0x2A BLE-anchor/0x30 steps/0x31 activity/0x37 bark/0x40 heart-rate/0x41 SpO2. MSG_CONFIGURATION TLV loop (35+ config keys: moduleNumber, firmware, IMEI, ICCID, bleMac, interval/petMode, deviceName, URLs, APN, server, alerts, bark, MSISDN, network info). MSG_RESPONSE: lastLocation + KEY_RESULT. 2 commands (TYPE_CONFIGURATION with optional custom hex or default 0xF0 query, TYPE_FIRMWARE_UPDATE Nano-model only via MSG_SYSTEM_CONTROL 0x30 + OTA URL). Java source, encoder, and old Java protocol tests moved to `archived-protocols/minifinder2/`. |
| `ulbotech` | Migrated to `drivers/ulbotech.groovy` covering dual binary/text protocol on port 5072. Single `scriptedFrame` variant handles both: binary frames are 0xF8-delimited with escape sequences (0xF7+0x00→0xF7, 0xF7+0x0F→0xF8), text frames terminate at '#'. Binary decode: IMEI from `readHex(8).substring(1)`, timestamp from 4-byte BE unsigned + 946684800 (2000-01-01 epoch) with MSB mask. TLV loop (stops at `> 3` bytes for trailing CRC+F8): GPS (lat/lon/speed/course/hdop validity), LBS (9-byte 2-cid or 11-byte 4-cid), STATUS (ignition bit 9 + alarm bitmask), ODOMETER, ADC (4-channel: power/temp/battery/adc + 5 fuel channels), GEOFENCE, OBD2/OBD2_ALARM (mode+paramLen nibble encoding via ObdDecoder), FUEL, HARSH_DRIVER (8 behavior bits), CANBUS (2-byte length), J1708 (mark byte len/type split, pid + 256 for type-3), VIN, RFID (driver ID + authorized byte), EVENT. Binary ACK: `F8 01 FE [hdr0 hdr1] [CRC16-XMODEM of 01 FE hdr0 hdr1] F8`. Text decode: regex groups for IMEI/HHMMSS/DDMMYY/command, ACK `*TS01,ACK:XXXX#` (CRC over bytes 1..end-2 of input), position via lastLocation with KEY_RESULT. 1 command (TYPE_CUSTOM). Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/ulbotech/`. |
| `bce` | Migrated to `drivers/bce.groovy` covering binary batch protocol on port 5080. `scriptedFrame` discards the stateless `#BCE#\r\n` 7-byte handshake (detected via leading ASCII match, since per-connection state isn't available) and otherwise scans chunk-by-chunk (ShortLE length + 2) accumulating a byte-sum checksum until it matches the trailing byte. Decode: IMEI via `readLongLE()` formatted `%015d`, outer chunk loop (type 0xA5 MSG_ASYNC_STACK or 0xA0 MSG_TIME_TRIGGERED via `buf.slice(chunkLen)`, unknown type aborts with null), inner struct loop (`chunk.slice(structLen)`, struct only carries data when `(rawTime & 0x0F) == 7`). Timestamp `(rawTime >> 4 << 1) + 0x47798280` (2008-01-01 epoch) with rollover correction. Mask chain: ShortLE words read while bit 15 set, up to 4 groups — mask1 (GPS via `Float.intBitsToFloat(readIntLE())`, sat/hdop nibbles, input, 8 ADC channels, fuel1/fuel2, cell tower), mask2 (OBD scalars: fuel used/level/RPM/hours/odometer/coolant/engine load/odometer-service/fuel consumption), mask3 (fuel consumption, 3-byte manual axle weight, driver ID via `readLongLE()`, dallas temp, 4 fuel+fuelTemp pairs), mask4 (max accel/braking/cornering ÷50, 4 temperature+skip blocks, driver1/driver2 16-byte trimmed strings, odometer). Positions emitted via `ctx.emit()`; non-GPS structs with attributes use `ctx.lastLocation()`. ACK (13 bytes) for MSG_ASYNC_STACK: IMEI LE + `02 00` + `0x19` + confirmKey + byte-sum checksum. 1 command (TYPE_OUTPUT_CONTROL, encoding not yet ported). Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/bce/`. |
| `fifotrack` | Migrated to `drivers/fifotrack.groovy` covering ASCII comma-delimited protocol on port 5124. `scriptedFrame` finds the first comma, parses the embedded decimal length field between `$$` and that comma, computes total frame length, and uses `frameResult()` to consume the trailing 2-byte CRLF delimiter without delivering it. Decode converts the whole buffer to a string (`readString`) then locates the message type via three chained `indexOf(',')` calls, exactly mirroring the Java byte-offset logic, before dispatching: `B*` → `decodeResult` (regex-matched `KEY_RESULT`, no location data, faithfully matching the Java behavior of not calling `getLastLocation`); `A03` → `decodeLocationNew` (cell/battery/hex-status fields, then either a GPS branch or a `|`-delimited WiFi-AP branch via regex alternation, ACK echoes index + current UTC timestamp); legacy (`A00`/`A01`/etc.) → `decodeLocation` (full GPRMC-like fields, hex status bitfields via `BitUtil`, pipe-delimited ADC array, RFID-or-card heuristic via hex-digit regex, pipe-delimited sensor array). All three decoders use hand-translated regexes derived field-by-field from the Java `PatternBuilder` definitions (verified against `PatternBuilder`'s exact fragment-substitution rules) rather than reusing `PatternBuilder`/`Parser`, since those aren't exposed to driver scripts. `D05`/`D06` (photo request ack / binary photo chunk) intentionally return null without any photo-buffer bookkeeping — like `gps103`, the DSL has no equivalent of Java's `writeMediaFile()`, so chunked photo retrieval can't be completed even though the `TYPE_REQUEST_PHOTO` command can still be sent. 2 commands (TYPE_CUSTOM, TYPE_REQUEST_PHOTO). Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/fifotrack/`. |
| `galileo` | Migrated to `drivers/galileo.groovy` covering binary TLV protocol on port 5034. `scriptedFrame` distinguishes Iridium frames (header 0x01 + fixed 3-byte `0x01001c` marker at offset 3, length = 3 + ShortBE at offset 1) from regular tag-loop frames (length = 5 + ShortLE-masked-0x7fff at offset 1), deriving the 3-byte marker from a 4-byte BE read right-shifted 8 bits since `BufReader`/`FrameBuffer` have no medium-read method. Decode dispatches on header: 0x01 non-Iridium → `decodePositions` (repeated-tag detection starts a new position record mid-frame, tracked via a `Set` that's cleared on repeat; positions collected only when a fix exists or a result-only login response falls back to `lastLocation`; emits all positions via `ctx.emit()` and ACKs with the trailing checksum); 0x01 Iridium → `decodeIridiumPosition` (fixed envelope fields, signed-degree-plus-minutes coordinates via `BitUtil.check` sign flags, then either the same tag loop or, when the `extended` config flag is set, a bit-packed "minimal data set" + compact tag-index array via `decodeCompressedTags`); 0x08 → `decodeCompressedPositions` (repeated minimal-data-set + compact-tag-array records until 2 bytes remain). Shared `decodeTag`/`decodeTagOther` cover the full CAN/ADC/fuel/userdata tag ranges and named tags (HW/FW version, odometer, RPM, fuel, driver ID, status/IO bitmasks, free-text result/command tags). Photo packets (header 0x07) intentionally return null — consistent with `gps103`/`fifotrack`, no `writeMediaFile()` equivalent exists, and photo capture isn't in the supported command list. 2 commands (TYPE_CUSTOM, TYPE_OUTPUT_CONTROL) via shared `encodeText` with CRC16-Modbus trailer. Java source, frame decoder, encoder, and old Java protocol tests moved to `archived-protocols/galileo/`. |
| `tzone` | Migrated to `drivers/tzone.groovy` covering binary `0x2424`-magic protocol on port 5029, framed via `readLengthField(2, 2, 2)`. Decode heavily branches on the "hardware" model field: `decodeGps` covers per-model GPS block layouts (0x40A signed-flag coordinates; 0x413 status/HDOP/altitude/knots-speed layout; default odometer+flags-encoded course/sign/validity layout; 0x10A/0x10B coordinate scale), LBS block covers 0x10A/0x10B/0x406 single-tower, 0x407 BCD-MCC/MNC multi-tower, and 0x40A BCD multi-tower with optional GSM/LTE signal-info subtype (0b000 vs 0b110 layouts), and the status block covers 0x40A (alarm/terminal/RSSI/GSM/battery/signed temperature/humidity) vs. the shared default layout (alarm/terminal/IO/RSSI/GSM/battery/power/ADC for non-0x407, or temperature/humidity/light-sensor for 0x407). Trailing blocks: 0x10B adds RFID card readers (`decodeCards`, faithfully replicating the original Java's `index`-never-incremented quirk where all parsed cards overwrite a single `"card1"` key) plus passenger counters (`decodePassengers`); 0x153/0x406 add sensor tag readings or a single RFID string (`decodeTags`); 0x40A adds a WiFi AP list. ACK echoes the trailing 2-byte index plus a UTC timestamp string when configured. `CellTower.fromLacCid(Config, ...)` has no driver-script equivalent (no raw `Config` access from `DecodeContext`), so the 0x10A/0x10B/0x406 single-tower case hardcodes mcc=0/mnc=0 via `CellTower.from(0, 0, lac, cid)`, matching the existing `gps103` precedent for the same gap. The status block's length-bounded skip-to-block-end needs to seek *backward* for at least one real, currently-passing test vector (declared block length smaller than the cumulative bytes the original Java code actually reads before snapping back via `readerIndex(blockEnd)`); this surfaced a genuine `BufReader.skip()` limitation (forward-only `ByteBuf.skipBytes`), fixed by switching its implementation to `buf.readerIndex(buf.readerIndex() + n)` so the established `targetRemaining` skip-to-block-end idiom supports negative deltas everywhere it's used (including `galileo.groovy`). Java source and old Java protocol test moved to `archived-protocols/tzone/`. |
| `ruptela` | Migrated to `drivers/ruptela.groovy` covering binary length-field-framed protocol on port 5046 (`readLengthField(0, 2, 2)`). IMEI is read as a raw 8-byte big-endian long and formatted `%015d`. MSG_RECORDS (1) and MSG_EXTENDED_RECORDS (68) both decode batches of position records via `ctx.emit()`: each record reads a Unix-epoch-seconds timestamp, skips a timestamp-extension byte, and either decodes GPS (lon/lat/alt/course/sats/speed/hdop) or skips 8 bytes and calls `ctx.lastLocation()`. MSG_EXTENDED_RECORDS adds a merge-record mechanic where `currentRecord > 0` replaces the last emitted position to accumulate IO parameters across split records. Four IO-size passes (1/2/4/8 bytes) per record feed a 40-case `decodeParameter` switch that maps known IDs to named Position attributes (power/battery/temp/fuel/RPM/ADC/odometer/alarms) and falls through to generic `ioN` keys. Driver ID is reconstructed by pairing two 8-byte long IO params (io126+io127 for CAN driver, io155+io156 for TCO driver) and decoding the 16-byte concatenation as US-ASCII. Tag IDs pair io760+io761 into a hex string. MSG_DTCS (9) decodes diagnostic trouble code records (lon/lat/timestamp/archive-flag/5-char DTC string). MSG_FILES (37, photo) returns null — no `writeMediaFile()` equivalent in driver scripts. MSG_IDENTIFICATION (15) and MSG_HEARTBEAT (16) ACK with `encodeContent(type, [0x01])`. Fixed binary ACKs: `0002640113bc` for records, `00026d01c4a4` for DTCs. `encodeContent` wraps payload with `(1+len)` length field, `100+type` type byte, and CRC16-Kermit over the framed content. 10 commands: CUSTOM (hex raw or ASCII via MSG_SMS_VIA_GPRS), ENGINE_STOP/RESUME ("pass immobilizer 10"/"pass resetimmob" text via MSG_SMS_VIA_GPRS), REQUEST_PHOTO (MSG_FILES subtype 1), CONFIGURATION (MSG_DEVICE_CONFIGURATION text), GET_VERSION (empty MSG_DEVICE_VERSION), FIRMWARE_UPDATE ("|FU_STRT*\r\n" via MSG_FIRMWARE_UPDATE), OUTPUT_CONTROL (two ints via MSG_SET_IO), SET_CONNECTION ("host,port,TCP" via MSG_SET_CONNECTION), SET_ODOMETER (int via MSG_SET_ODOMETER). Java source, encoder, and old Java protocol test moved to `archived-protocols/ruptela/`. |

## Remaining Active Java Protocols

19 Java protocol implementations remain under `src/main/java/org/traccar/protocol/`.

### Blocked on DSL gaps

| Protocol | Blocker |
| --- | --- |
| `osmand` | HTTP request/response — no HTTP handler support in driver DSL |
| `globalstar` | HTTP request/response — no HTTP handler support in driver DSL |

### Large / complex — audit required before migration

| Protocol | Decoder LOC | Notes |
| --- | --- | --- |
| `gt06` | 1632 | Massive binary protocol; batch, commands, multiple hardware variants |
| `jt808` | 1632 | Chinese national standard; batch, many message types, encryption variants |
| `suntech` | 981 | Many ST-xxx model variants; complex field layout per model |
| `navis` | 730 | Complex binary TLV with many sub-types |
| `atrack` | 729 | Multi-record binary batch; custom framing |
| `teltonika` | 726 | Complex binary batch with codec variants |
| `aplicom` | 648 | Multi-application binary TLV |
| `castel` | 645 | Many hardware variants |
| `tk103` | 614 | Mixed text/binary; many clone variants |

### Simpler / ready to migrate

| Protocol | Decoder LOC | Notes |
| --- | --- | --- |
| `meitrack` | 597 | Text CSV protocol |
| `t55` | 593 | NMEA-based text protocol |
| `totem` | 573 | Text CSV protocol |
| `eelink` | 509 | Binary with standard length-field framing |
| `meiligao` | 507 | Binary with header/checksum framing |
| `jt600` | 507 | Binary; simpler sibling of jt808 |
| `mta6` | 309 | HTTP-based — blocked on DSL HTTP support |
| `gl200` | 58 (dispatcher) | Text CSV; delegates to Gl200Text/Gl200Binary sub-decoders (separate files) |
